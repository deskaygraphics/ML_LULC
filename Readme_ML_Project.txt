Readme
1. The size of the dataset is not big so kindly download it onto your local machine.

2. When running the code on the colab environment, there not need to set a data path.

3. Just run the code as written, without changing the default name of the data, it will allow you select the data from your local machine.

4. Upload the geojson data using the first code line and upload the image second.

5. everything should work.